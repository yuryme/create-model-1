---
sp-id: NNN
title: <Краткое название>
status: draft
author: analyst
created: YYYY-MM-DD
updated: YYYY-MM-DD
depends-on: []
---

# ТЗ NNN: <Заголовок>

> **Это шаблон.** При создании нового ТЗ скопировать в `sp-NNN-<short-name>.md` и заполнить.
> `NNN` — порядковый номер (`001`, `002`, ...). Свободный номер — посмотреть в этом каталоге.

---

## 1. Контекст

<Бизнес-задача словами. Кто, зачем, в каких сценариях будет этим пользоваться. От чего отказались и почему.>

## 2. Scope

### В составе

- ...

### НЕ в составе (явно, чтобы не было разночтений)

- ...

## 3. Метаданные к созданию — сводка

Высокоуровневый список. Каждая строка — один метаобъект.

| Вид | Name | Title | Назначение |
|---|---|---|---|
| catalog | <name> | <Название> | <1 предложение> |
| operation | <name> | <Название> | <1 предложение> |
| ... | ... | ... | ... |

## 4. Метаданные — детали

### 4.1. `<kind>/<name>` — <Title>

#### Memo

<Одно-два предложения для поля Memo.>

#### Колонки шапки

| Name | Title | DataType | Required | Unique | PrimaryKey | Formula / itemsSource | Memo |
|---|---|---|---|---|---|---|---|
| name | Наименование | string | ✓ |   |   |   | Основное наименование |

#### Табличные части

##### `<table_name>` — <Title табличной части>

| Name | Title | DataType | Required | Formula | Memo |
|---|---|---|---|---|---|
| ... | ... | ... | ... | ... | ... |

#### Связи и ссылки

- Колонка `<name>` ссылается на `catalog/<another>` (через `DataTypeUid` из `metadata/system/dataTypes.json` или из `system/dataTypes.json` после создания нужного объекта).
- ...

#### Проведение по регистрам (если применимо)

| Регистр-приёмник | Направление | Источник | Условие | Колонки регистра ← Выражения |
|---|---|---|---|---|
| `<records.<name>>` | Plus | `header` или `detailTables[i]` или RecordsSource | `<JS-условие>` или пусто | `period ← $h.date`, `amount ← $r.quantity * $r.price`, ... |

Если нужен `RecordsSources` — описать каждый отдельным подразделом со скриптом-источником (или хотя бы его задачей):

##### RecordsSource: `<source_name>`

- **Назначение:** ...
- **Логика:** группировка/преобразование `$t.<table>` и/или `$h.<column>`, возвращает DataTable с колонками `[...]`.

#### Команды (если нужны)

| Name | Title | TableUid (header/<table>) | Kind | Что делает |
|---|---|---|---|---|
| ... | ... | ... | 0 (программируемая) | ... |

#### Отчёты (если нужны)

- Тип (`data_view`, `excel_report`, ...).
- Какие индикаторы, какие источники данных, какие фильтры.

#### Прочие особенности

- ...

### 4.2. `<kind>/<name>` — <следующий объект>

(аналогично)

## 5. Меню

Добавить в меню `<menu_name>`:
- Группа «<Название группы>» (создать новую или существующую): ссылка на `/app#/data-objects/<kind>/<name>` с заголовком `<Title>`.
- ...

## 6. Зависимости и порядок реализации

- Создать сначала `<kind>/<name1>`, потому что на него ссылается `<kind>/<name2>`.
- ...

## 7. Чек-лист приёмки

Обязательные пункты (всегда):

- [ ] Все метаобъекты из раздела 3 созданы и валидируются по соответствующим JSON-схемам.
- [ ] Имена `Name` — латиница `snake_case`, ≤30 символов (ADR-001).
- [ ] `MetaObjectKindUid` и `DataTypeUid` — взяты из **нашего** `project/metadata/system/`, не из `reference/` (ADR-002).
- [ ] `Memo` заполнено на каждом новом объекте и каждой новой колонке.
- [ ] Свежие UUID v4 на каждом `Uid` (объект, колонки, табличные части, команды, источники).
- [ ] У объектов с `Kind` где `IsReference = true` — добавлена запись в `project/metadata/system/dataTypes.json`.

Пункты под конкретное ТЗ (заполняет аналитик):

- [ ] Проведение по регистру `<name>` работает: при создании документа с <условие> — формируются записи с <ожиданием>.
- [ ] Команда `<name>` отрабатывает: <сценарий проверки>.
- [ ] Отчёт `<name>` показывает <ожидаемые данные> на тестовых данных.
- [ ] ...

## 8. Открытые вопросы / решения

(Если что-то ещё не решено — фиксировать здесь, чтобы PM/Инженер могли поднять.)

- ...

## 9. История изменений

- **YYYY-MM-DD** — создано (draft).
- **YYYY-MM-DD** — review.
- **YYYY-MM-DD** — approved.
- **YYYY-MM-DD** — implemented.
